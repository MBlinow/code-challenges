using System;

namespace C__HackerRank_Harness
{
    public static class Solution
    {
        public static string Solve(string[] lines)
        {
            // Placeholder: Echo the first line or "No input" if empty
            return lines.Length > 0 ? lines[0] : "No input";
        }
    }
}